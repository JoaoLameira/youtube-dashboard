import React from "react";
import { MainView } from "~/components/MainView";

const Home = () => {
  return (
    <MainView className="flex justify-center items-center">
      Select one video from the list!
    </MainView>
  );
};

export default Home;
